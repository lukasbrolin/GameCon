@using DataLayer.Models
@model List<Message>

@{
    ViewData["Title"] = "Chat";
}
<div class="container">
<h1>Chat</h1>

@foreach (var item in Model)
{
    @item.TimeStamp.Date
    @item.Content
}
</div>